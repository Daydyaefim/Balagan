# 🚀 ПРОСТАЯ УСТАНОВКА - БЕЗ КОМАНДНОЙ СТРОКИ!

## Развертывание через n8n UI за 15 минут

**БЕЗ Nginx! БЕЗ SSH! БЕЗ командной строки!**

Всё делается через веб-интерфейс n8n и панель управления Beget! 🎉

---

## 📋 Что вам нужно

- ✅ Beget VPS с установленной n8n (через маркетплейс)
- ✅ Telegram бот (токен от @BotFather)
- ✅ ESP32 с кодом UgAgro_95_refactored

---

## 🎯 Шаг 1: Создание директории для данных (через Beget панель)

### Вариант А: Через Beget File Manager

1. Войдите в **панель управления Beget**
2. Откройте **File Manager** (менеджер файлов)
3. Перейдите в вашу домашнюю директорию (обычно `/home/username/`)
4. Создайте новую папку: **greenhouse-data**
5. Внутри `greenhouse-data` создайте пустые файлы:
   - `ugagro_readings.json`
   - `telegram_alert_states.json`
   - `ugagro_alerts_history.json`
   - `cleanup_log.json`

### Вариант Б: Пропустить этот шаг!

n8n автоматически создаст файлы при первом запуске workflows. Просто запомните путь к вашей домашней директории.

**Как узнать путь?**
- В Beget панели это обычно: `/home/ваш_логин/`
- Например: `/home/john/greenhouse-data/`

---

## 🎯 Шаг 2: Получение Telegram токена

### 2.1 Создание бота

1. Откройте Telegram
2. Найдите **@BotFather**
3. Отправьте команду: `/newbot`
4. Придумайте имя бота (например: "UgAgro Monitor")
5. Придумайте username (например: "ugagro95_bot")
6. **Скопируйте токен** (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2.2 Получение Chat ID

1. Найдите в Telegram бота **@userinfobot**
2. Отправьте ему `/start`
3. **Скопируйте ваш ID** (число, например: `123456789`)

### 2.3 Активация бота

1. Найдите вашего созданного бота в Telegram
2. Отправьте ему `/start`

---

## 🎯 Шаг 3: Настройка n8n через UI

### 3.1 Открыть n8n

Откройте браузер и перейдите:
```
http://ваш-vps-ip:5678
```

Или если у вас настроен домен:
```
https://n8n.ваш-домен.ru
```

Войдите с логином и паролем от n8n.

---

### 3.2 Создание Credentials

#### MQTT Credential

1. В n8n нажмите на **⚙️** (Settings) в левом меню
2. Выберите **Credentials**
3. Нажмите **+ Add Credential**
4. Найдите и выберите **MQTT**
5. Заполните:
   - **Credential Name**: `MQTT Broker`
   - **Protocol**: `mqtt`
   - **Host**: `broker.emqx.io`
   - **Port**: `1883`
   - **Username**: (оставить пустым)
   - **Password**: (оставить пустым)
6. Нажмите **Save**

#### Telegram Credential

1. **+ Add Credential**
2. Найдите **Telegram API**
3. Заполните:
   - **Credential Name**: `Telegram Bot`
   - **Access Token**: вставьте ваш токен от BotFather
4. Нажмите **Save**

---

### 3.3 Импорт Workflows

**Скачайте workflows на компьютер:**

1. Перейдите на GitHub:
   ```
   https://github.com/Daydyaefim/Balagan/tree/claude/n8n-esp32-greenhouse-integration-0182vddx43zfdqyfMHo57bfV/n8n-greenhouse-integration/workflows
   ```

2. Скачайте все 6 файлов:
   - `01-mqtt-data-pipeline.json`
   - `02-critical-alerts-monitor.json`
   - `03-telegram-callback-handler.json`
   - `04-web-dashboard-api.json`
   - `05-data-rotation-cleanup.json`
   - `06-web-dashboard-simple.json` ⭐ **НОВЫЙ! Упрощенный дашборд**

**Импортируйте каждый workflow:**

1. В n8n нажмите **Workflows** в левом меню
2. Нажмите **+ Add Workflow** → **Import from File**
3. Выберите скачанный JSON файл
4. Workflow откроется в редакторе

---

### 3.4 Настройка каждого Workflow

**Для КАЖДОГО workflow выполните:**

#### Шаг 1: Применить Credentials

Найдите ноды (узлы) с иконкой **🔑** (ключ):

- **MQTT Trigger** / **MQTT** → выберите `MQTT Broker`
- **Telegram** / **Telegram Trigger** → выберите `Telegram Bot`

Кликните на узел → в параметрах выберите credential → Save

#### Шаг 2: Изменить пути к файлам

**ВАЖНО!** Во ВСЕХ workflows замените пути к файлам:

**Найдите все ноды типа:**
- "Читать БД"
- "Сохранить БД"
- "Читать Данные"
- "Читать Состояния"
- и т.д.

**В каждом измените `filePath`:**

**Было:**
```
/data/ugagro_readings.json
```

**Стало:**
```
/home/ваш_логин/greenhouse-data/ugagro_readings.json
```

**Замените `ваш_логин` на ваш реальный логин в Beget!**

Например:
```
/home/john/greenhouse-data/ugagro_readings.json
```

**Список путей для замены:**
- `/data/ugagro_readings.json` → `/home/ваш_логин/greenhouse-data/ugagro_readings.json`
- `/data/telegram_alert_states.json` → `/home/ваш_логин/greenhouse-data/telegram_alert_states.json`
- `/data/ugagro_alerts_history.json` → `/home/ваш_логин/greenhouse-data/ugagro_alerts_history.json`
- `/data/cleanup_log.json` → `/home/ваш_логин/greenhouse-data/cleanup_log.json`

#### Шаг 3: Добавить TELEGRAM_CHAT_ID

В **Workflow #2 (Critical Alerts Monitor)** и **Workflow #3 (Telegram Callback Handler)**:

Найдите ноды **"Отправить в Telegram"** и подобные.

В параметре `chatId` замените:
```
={{ $env.TELEGRAM_CHAT_ID }}
```

на ваш реальный Chat ID:
```
123456789
```

#### Шаг 4: Сохранить Workflow

После всех изменений:
1. Нажмите **Save** (в правом верхнем углу)
2. Дайте workflow понятное имя (если нужно)

---

### 3.5 Активация Workflows

**После настройки ВСЕХ workflows, активируйте их:**

1. Откройте workflow
2. Переключите **Active** (тумблер в правом верхнем углу) → должен стать зеленым
3. Повторите для всех 6 workflows

**Порядок активации:**

1. ✅ **01 - MQTT Data Pipeline** (первым!)
2. ✅ **02 - Critical Alerts Monitor**
3. ✅ **03 - Telegram Callback Handler**
4. ✅ **04 - Web Dashboard API**
5. ✅ **05 - Data Rotation & Cleanup**
6. ✅ **06 - Web Dashboard (Simple)** ⭐ **НОВЫЙ!**

---

## 🌐 Шаг 4: Доступ к Веб-Дашборду

### Где находится дашборд?

После активации **Workflow #6 (Web Dashboard Simple)**, дашборд доступен по адресу:

```
http://ваш-vps-ip:5678/webhook/dashboard
```

Или если настроен домен:

```
https://n8n.ваш-домен.ru/webhook/dashboard
```

**Вот и всё!** 🎉

### Что вы увидите:

- ✅ 4 карточки сенсоров с цветовой индикацией
- ✅ Дополнительная информация (уличные датчики, оборудование)
- ✅ 4 графика Chart.js (температура, влажность, вода, ветер)
- ✅ Кнопка "Обновить" для актуальных данных

### Особенности:

- 📊 Все данные берутся из JSON файла (за последние 24 часа)
- 🔄 Обновление по кнопке (перезагрузка страницы)
- 🎨 Красивый дизайн на Bootstrap 5
- 📱 Адаптивная верстка

---

## ✅ Проверка работоспособности

### 1. Проверка MQTT данных

1. Откройте **Workflow #1 (MQTT Data Pipeline)**
2. Посмотрите на **Executions** (внизу)
3. Должны появляться новые записи каждые ~30 секунд (когда ESP32 отправляет данные)

### 2. Проверка Telegram бота

1. Создайте критическую ситуацию (например, отключите датчик)
2. Через ~20 секунд должно прийти Telegram сообщение с кнопкой
3. Нажмите "✅ Принято к сведению"
4. Оповещения должны прекратиться

### 3. Проверка дашборда

1. Откройте `http://ваш-ip:5678/webhook/dashboard`
2. Должны отобразиться текущие показания
3. Если данных нет - подождите пока ESP32 отправит данные (до 30 сек)
4. Нажмите "🔄 Обновить" для актуализации

---

## 🛠️ Устранение Неполадок

### Дашборд не открывается

**Проблема:** `Cannot GET /webhook/dashboard`

**Решение:**
1. Убедитесь, что **Workflow #6** активен (зеленый тумблер)
2. Откройте workflow и нажмите "Execute Workflow"
3. Проверьте URL - должен быть `/webhook/dashboard` (без `/webhook-test/`)

### Нет данных на дашборде

**Проблема:** Карточки показывают `--` или `0`

**Решение:**
1. Проверьте, что ESP32 отправляет данные в MQTT
2. Проверьте **Workflow #1** - есть ли executions?
3. Проверьте путь к файлу в **Workflow #6** → нода "Читать Данные"
4. Убедитесь, что файл `/home/ваш_логин/greenhouse-data/ugagro_readings.json` существует

### Telegram бот не отвечает

**Решение:**
1. Убедитесь, что **Workflow #2** и **Workflow #3** активны
2. Проверьте токен в Telegram Credential
3. Проверьте Chat ID в нодах Telegram
4. Напишите боту `/start` еще раз

### Ошибка "File not found"

**Решение:**
1. Создайте файлы через Beget File Manager (см. Шаг 1)
2. Или дождитесь - n8n создаст их автоматически при первой записи

---

## 📊 Дополнительные возможности

### Автоматическое обновление дашборда

Хотите автоматическое обновление каждые 30 секунд?

Добавьте в конец HTML (в ноде "Генерация HTML"):

```javascript
<script>
setTimeout(() => location.reload(), 30000); // Обновление каждые 30 сек
</script>
```

### Добавление своих виджетов

Отредактируйте ноду "Генерация HTML" в **Workflow #6** - там весь HTML код.

Можете добавить:
- Больше карточек
- Больше графиков
- Свои стили CSS
- Дополнительную информацию

---

## 📱 Мобильная версия

Дашборд полностью адаптивен! Откройте на телефоне:

```
http://ваш-ip:5678/webhook/dashboard
```

Для удобства сохраните в закладки или на главный экран.

---

## 🎯 Итоговый Checklist

- [ ] Beget VPS с n8n работает
- [ ] Telegram бот создан, получен токен
- [ ] Получен Chat ID
- [ ] В n8n созданы 2 credentials (MQTT, Telegram)
- [ ] Импортированы все 6 workflows
- [ ] В каждом workflow настроены credentials
- [ ] Во всех workflows изменены пути к файлам (`/home/ваш_логин/...`)
- [ ] В Telegram нодах указан Chat ID
- [ ] Все 6 workflows активированы (зеленые)
- [ ] Дашборд открывается по `/webhook/dashboard`
- [ ] ESP32 отправляет данные в MQTT
- [ ] Telegram бот отвечает

---

## 🎉 Готово!

**Теперь у вас:**
- ✅ Работающий веб-дашборд доступный по `http://ip:5678/webhook/dashboard`
- ✅ Telegram бот с оповещениями
- ✅ Автоматическое хранение данных
- ✅ Автоматическая очистка старых данных

**И всё это БЕЗ командной строки!** 🚀

---

## 📞 Нужна помощь?

- 📖 [Полная документация](../README.md)
- 📄 [Структура MQTT данных](MQTT_DATA_STRUCTURE.md)
- 💬 GitHub Issues

**Приятного мониторинга вашей теплицы!** 🌱
