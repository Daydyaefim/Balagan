# 🚀 Быстрый Старт - n8n Greenhouse Integration

## Минимальная настройка за 10 минут

### Шаг 1: Клонирование и подготовка (1 мин)

```bash
git clone https://github.com/Daydyaefim/Balagan.git
cd Balagan/n8n-greenhouse-integration

# Создать директорию для данных
mkdir -p data
chmod 777 data

# Скопировать и настроить .env
cp .env.example .env
nano .env
```

### Шаг 2: Настройка .env (2 мин)

Отредактируйте `.env` файл:

```env
# Измените пароль для n8n
N8N_BASIC_AUTH_PASSWORD=ваш_надежный_пароль

# Добавьте данные Telegram бота
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
```

**Как получить Telegram токен:**
1. Откройте Telegram, найдите @BotFather
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

**Как получить Chat ID:**
1. Найдите в Telegram @userinfobot
2. Отправьте `/start`
3. Скопируйте ваш ID

### Шаг 3: Запуск с Docker (1 мин)

```bash
docker-compose up -d
```

Проверка статуса:
```bash
docker-compose ps
```

Должно быть запущено:
- `n8n-greenhouse` (порт 5678)
- `nginx-greenhouse` (порт 80)

### Шаг 4: Импорт workflows (3 мин)

1. Откройте браузер: `http://localhost:5678`
2. Войдите (логин: `admin`, пароль из `.env`)
3. Перейдите в **Workflows** → **Import from File**
4. Импортируйте все 5 workflows из папки `workflows/`:
   - 01-mqtt-data-pipeline.json
   - 02-critical-alerts-monitor.json
   - 03-telegram-callback-handler.json
   - 04-web-dashboard-api.json
   - 05-data-rotation-cleanup.json

### Шаг 5: Настройка credentials (2 мин)

#### MQTT

1. **Settings** → **Credentials** → **New**
2. Тип: **MQTT**
3. Параметры:
   - Host: `broker.emqx.io`
   - Port: `1883`
   - Остальное оставить пустым
4. **Save**

#### Telegram Bot

1. **Settings** → **Credentials** → **New**
2. Тип: **Telegram API**
3. Access Token: вставить из `.env`
4. **Save**

### Шаг 6: Применение credentials к workflows (1 мин)

Для каждого workflow:
1. Откройте workflow
2. Кликните на ноды с credentials (MQTT, Telegram)
3. Выберите созданные credentials
4. **Save** workflow

### Шаг 7: Активация workflows (1 мин)

Для каждого workflow:
1. Откройте workflow
2. Переключите **Active** в правом верхнем углу

✅ **Готово!**

---

## 📊 Проверка работоспособности

### Проверка веб-дашборда

Откройте браузер: `http://localhost`

Должны увидеть:
- 4 карточки сенсоров
- 4 графика
- Статус подключения

### Проверка MQTT данных

ESP32 должен публиковать данные в топик `greenhouse/esp32/all_sensors`.

Проверить можно в workflow #1:
1. Откройте **01 - MQTT Data Pipeline**
2. Нажмите **Execute Workflow**
3. Должно появиться сообщение с данными

### Проверка Telegram бота

1. Напишите боту `/start`
2. Если нет критических показаний, искусственно создайте (измените пороги в workflow #2)
3. Должно прийти сообщение с кнопкой "✅ Принято к сведению"

---

## 🔧 Логи и отладка

### Логи n8n

```bash
docker logs n8n-greenhouse -f
```

### Логи Nginx

```bash
docker logs nginx-greenhouse -f
```

### Проверка данных

```bash
cat data/ugagro_readings.json
cat data/telegram_alert_states.json
cat data/ugagro_alerts_history.json
```

---

## 🛑 Остановка и удаление

### Остановка

```bash
docker-compose stop
```

### Полное удаление

```bash
docker-compose down -v
rm -rf data/*
```

---

## 🆘 Проблемы?

### n8n не стартует

```bash
# Проверить логи
docker logs n8n-greenhouse

# Перезапустить
docker-compose restart n8n
```

### Веб-дашборд не открывается

```bash
# Проверить Nginx
docker logs nginx-greenhouse

# Проверить файлы
ls -la web-dashboard/
```

### Telegram бот не отвечает

1. Проверьте токен в `.env`
2. Проверьте, что написали боту `/start`
3. Проверьте credentials в workflow #2
4. Проверьте, что workflow #2 активен

---

## 📚 Дальнейшие шаги

- Прочитайте полную документацию: [README.md](../README.md)
- Настройте дополнительные Telegram команды
- Настройте экспорт в CSV
- Интегрируйте с Grafana

---

**🎉 Поздравляем! Ваша система мониторинга теплицы готова к работе!**
