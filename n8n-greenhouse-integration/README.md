# 🌱 n8n Greenhouse Integration для UgAgro 95

Полная интеграция n8n для автономной системы управления теплицей ESP32 UgAgro_95_refactored с PostgreSQL хранилищем.

## 🚀 Быстрый Старт

### 📚 Руководства по настройке

- 👉 **[Настройка PostgreSQL](docs/POSTGRES_SETUP.md)** - основная база данных
- 👉 **[Настройка через PuTTY](docs/PUTTY_SETUP.md)** - SSH доступ к серверу
- 👉 **[Развертывание на Beget VPS](docs/BEGET_VPS_DEPLOYMENT.md)** - полное руководство для VPS

---

## 📋 Содержание

- [Обзор](#обзор)
- [Архитектура](#архитектура)
- [Требования](#требования)
- [Workflows](#workflows)
- [База Данных](#база-данных)
- [Telegram Бот](#telegram-бот)
- [Веб-дашборд](#веб-дашборд)
- [Установка](#установка)

---

## 🎯 Обзор

Эта интеграция предоставляет:

- ✅ **MQTT Data Pipeline** - прием и сохранение данных от ESP32 в PostgreSQL
- ✅ **Telegram Bot Commands** - команда `/status` для просмотра текущих данных
- ✅ **Critical Alerts** - мониторинг критических параметров каждые 20 секунд
- ✅ **Callback Handler** - обработка кнопок подтверждения и отключения оповещений
- ✅ **Web Dashboard** - визуализация данных с графиками Chart.js
- ✅ **Data Rotation** - автоматическая очистка старых данных (7 дней показания, 30 дней оповещения)

---

## 🏗️ Архитектура

```
ESP32 (UgAgro_95)
    ↓ MQTT (broker.emqx.io)
n8n Workflow #1: MQTT Data Pipeline
    ↓
PostgreSQL Database (ugagro_readings)
    ↓
┌──────────────────────────┬────────────────────────┐
│                          │                        │
Workflow: Telegram         Workflow:                Workflow:
Critical Alerts            Telegram Commands        Data Rotation
(каждые 20 сек)           (/status)                (ежедневно 02:00)
    ↓                          ↓                        ↓
Telegram Bot               Telegram Bot             Очистка старых данных
    ↓
Workflow: Telegram Callback Handler
(обработка кнопок)
```

---

## 📦 Требования

### Системные требования

- **n8n** v1.115.3+ (self-hosted)
- **PostgreSQL** v11+
- **Docker & Docker Compose** (рекомендуется)
- **MQTT Broker** (broker.emqx.io или свой)
- **Telegram Bot** (создать через @BotFather)

### ESP32

- Проект UgAgro_95_refactored запущен и подключен к MQTT
- MQTT топик: `greenhouse/esp32/all_sensors`

---

## 🔄 Workflows

### 01-mqtt-data-pipeline.json

**Триггер**: MQTT сообщение на топик `greenhouse/esp32/all_sensors`

**Функции**:
- Парсинг и валидация JSON payload от ESP32
- Сохранение данных в PostgreSQL таблицу `ugagro_readings`
- Обработка конфликтов (ON CONFLICT DO UPDATE)
- Поддержка 30+ параметров сенсоров

**Обязательные поля**: `timestamp`, `temperature`, `humidity`, `water_level`, `wind_speed`

### telegram-bot-commands.json

**Триггер**: Входящие сообщения Telegram

**Команды**:
- `/status` или `/статус` - отображает текущие показания теплицы
- Цветовая индикация: 🟢 норма, 🟡 предупреждение, 🔴 критично

**Отображаемые данные**:
- Температура (норма: 10-40°C)
- Влажность (норма: 20-85%)
- Уровень воды (мин: 5%)
- Скорость ветра (макс: 11 м/с)
- Все состояния оборудования (окна, вентиляторы, обогрев, насосы)

### telegram-critical-alerts.json

**Триггер**: Каждые 20 секунд

**Критические пороги**:
- 🔥 Температура > 40°C
- ❄️ Температура < 10°C
- 💧 Влажность > 85%
- 🏜️ Влажность < 20%
- 🚨 Уровень воды < 5%
- 💨 Скорость ветра > 11 м/с

**Функции**:
- Проверка последних показаний из PostgreSQL
- Сравнение с критическими порогами
- Отправка Telegram оповещений с inline кнопками
- State management (предотвращение спама после подтверждения)
- Поддержка отключения на время (mute)
- Автоматическая деактивация при возврате в норму

**Inline кнопки**:
- ✅ Подтвердить - останавливает оповещения
- 🔕 Отключить на 1ч - временное отключение

### telegram-callback-handler.json

**Триггер**: Callback query от Telegram (нажатие кнопок)

**Функции**:
- Обработка callback_data: `ack_<alert_type>` или `mute_<alert_type>_1h`
- Обновление PostgreSQL таблицы `telegram_alert_states`
- Установка `acknowledged_at` или `mute_until` timestamps
- Редактирование сообщения с подтверждением
- Отображение имени пользователя, подтвердившего оповещение

### 05-data-rotation-cleanup.json

**Триггер**: Cron (ежедневно в 02:00)

**Функции**:
- Вызов PostgreSQL функции `cleanup_old_data()`
- Удаление показаний старше 7 дней
- Удаление истории оповещений старше 30 дней
- Вывод статистики в консоль n8n

### 06-web-dashboard-simple.json

**Триггер**: Webhook GET `/webhook/dashboard`

**Функции**:
- Получение последних 100 показаний из PostgreSQL
- Подготовка данных для графиков (последние 6 часов)
- Генерация HTML страницы с Bootstrap 5 и Chart.js
- Автообновление каждые 30 секунд
- Адаптивный дизайн для мобильных устройств

**ВАЖНО**: PostgreSQL возвращает числа как строки, поэтому используется `parseFloat()` для конвертации перед вызовом `.toFixed()`.

---

## 🗄️ База Данных

### PostgreSQL Схема

#### ugagro_readings

Основная таблица с показаниями датчиков (7 дней ретенция).

**Основные поля**:
- `timestamp` (INTEGER, PRIMARY KEY) - Unix timestamp
- `timestamp_iso` (TIMESTAMP) - ISO формат времени
- `temperature`, `humidity`, `water_level`, `wind_speed` (DECIMAL) - основные сенсоры
- `pyrano` (DECIMAL) - солнечная радиация
- `solution_temperature`, `outdoor_temperature`, `outdoor_humidity` - дополнительные температуры
- `mat_temperature`, `mat_hum`, `mat_ec`, `mat_ph` - данные матов
- `window_position` (INTEGER) - позиция окна (0-100%)
- `fan_state`, `heat_state`, `pump_state`, `fog_state`, и другие (BOOLEAN) - состояния устройств
- `watering_mode`, `rad_sum`, `cycle_count` - режимы полива
- `last_watering_start_unix`, `force_watering_end_time_unix` - таймстампы полива

#### telegram_alert_states

Таблица состояний оповещений Telegram.

**Поля**:
- `alert_type` (VARCHAR, UNIQUE) - тип оповещения (high_temperature, low_humidity и т.д.)
- `is_active` (BOOLEAN) - активно ли оповещение
- `last_sent_at` (TIMESTAMP) - время последней отправки
- `acknowledged_at` (TIMESTAMP) - время подтверждения
- `mute_until` (TIMESTAMP) - отключено до времени
- `alert_count` (INTEGER) - счетчик отправок
- `current_value` (DECIMAL) - текущее значение параметра

**Предзаполненные типы**:
- `high_temperature`, `low_temperature`
- `high_humidity`, `low_humidity`
- `low_water_level`
- `high_wind_speed`

#### ugagro_alerts_history

История всех оповещений (30 дней ретенция).

**Поля**:
- `alert_type` (VARCHAR) - тип оповещения
- `alert_message` (TEXT) - текст сообщения
- `sensor_value` (DECIMAL) - значение сенсора при оповещении
- `severity` (VARCHAR) - уровень: 'warning' или 'critical'
- `acknowledged` (BOOLEAN) - было ли подтверждено
- `created_at` (TIMESTAMP) - время создания

### Функции PostgreSQL

#### cleanup_old_data()

Автоматическая очистка старых данных:
```sql
DELETE FROM ugagro_readings WHERE timestamp < EXTRACT(EPOCH FROM NOW() - INTERVAL '7 days');
DELETE FROM ugagro_alerts_history WHERE created_at < NOW() - INTERVAL '30 days';
```

### Индексы

- `idx_readings_timestamp` - быстрый поиск по времени
- `idx_alerts_history_created` - быстрый поиск истории

---

## 🤖 Telegram Бот

### Создание бота

1. Откройте Telegram и напишите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям, получите TOKEN
4. Получите CHAT_ID через @userinfobot (команда `/start`)

### Настройка credentials в n8n

1. **Settings** → **Credentials** → **New**
2. Тип: **Telegram API**
3. Параметры:
   - **Access Token**: `ваш_токен_от_BotFather`
   - **Name**: `UgAgro Telegram Bot`

### Команды

**`/status`** или **`/статус`**
```
🌱 UGAGRO 95 - СТАТУС ТЕПЛИЦЫ

📊 ОСНОВНЫЕ ПОКАЗАТЕЛИ:

🟢 Температура: 24.5°C
   └ Норма: 10-40°C

🟢 Влажность: 65.2%
   └ Норма: 20-85%

🟢 Уровень воды: 78.0%
   └ Минимум: 5%

🟢 Ветер: 3.2 м/с
   └ Максимум: 11 м/с

━━━━━━━━━━━━━━━━━━━━━
⏰ Обновлено: 18.01.2025, 15:30
```

### Критические оповещения

Пример оповещения:
```
🔥 КРИТИЧНО! Температура слишком высокая

📊 Текущее значение: 42.3°C
⚠️ Максимум: 40°C

🕒 Время: 18.01.2025, 15:45

━━━━━━━━━━━━━━━━━━━━━

📋 ВСЕ ПОКАЗАТЕЛИ:
🌡️ Температура: 42.3°C
💧 Влажность: 65.2%
💦 Уровень воды: 78.0%
💨 Ветер: 3.2 м/с

[✅ Подтвердить] [🔕 Отключить на 1ч]
```

После нажатия "✅ Подтвердить":
```
✅ Оповещение подтверждено

Тип: high_temperature
Пользователь: Ivan
Время: 18.01.2025, 15:46
```

---

## 📊 Веб-дашборд

### Доступ

```
https://your-domain.com/webhook/dashboard
```

### Функциональность

**Карточки сенсоров**:
- 🌡️ Температура (°C)
- 💧 Влажность (%)
- 💦 Уровень воды (%)
- 💨 Ветер (м/с)

**Графики Chart.js** (последние 6 часов):
- Линейный график температуры
- Линейный график влажности
- Линейный график уровня воды
- Линейный график скорости ветра

**Дополнительная информация**:
- Позиция окна (%)
- Состояния оборудования (иконки)
- Время последнего обновления
- Автообновление каждые 30 секунд

---

## 🚀 Установка

### Шаг 1: Инициализация PostgreSQL

```bash
# Через PuTTY SSH
docker ps  # найти имя контейнера PostgreSQL (обычно n8n-postgres-1)

# Выполнить SQL скрипт (путь на сервере Beget)
docker exec -i n8n-postgres-1 psql -U root -d n8n < /opt/beget/n8n/init-greenhouse-db.sql
```

**Важно:** Файл `init-greenhouse-db.sql` должен быть загружен на сервер в `/opt/beget/n8n/`

**Пути на сервере:**
- Файл SQL: `/opt/beget/n8n/init-greenhouse-db.sql`
- Исходный файл в репозитории: `n8n-greenhouse-integration/database/init-greenhouse-db.sql`

Подробная инструкция: [PUTTY_SETUP.md](docs/PUTTY_SETUP.md)

### Шаг 2: Создание credentials в n8n

#### PostgreSQL

1. **Settings** → **Credentials** → **New**
2. Тип: **Postgres**
3. Параметры:
   - **Name**: `Greenhouse PostgreSQL`
   - **Host**: `postgres` (Docker service name)
   - **Database**: `n8n`
   - **User**: `root`
   - **Password**: `your_postgres_password`
   - **Port**: `5432`

#### MQTT Broker

1. **Settings** → **Credentials** → **New**
2. Тип: **MQTT**
3. Параметры:
   - **Name**: `MQTT Broker`
   - **Protocol**: `mqtt`
   - **Host**: `broker.emqx.io`
   - **Port**: `1883`

#### Telegram Bot

См. раздел [Telegram Бот](#telegram-бот)

### Шаг 3: Импорт workflows

1. Откройте n8n UI: `http://localhost:5678`
2. Перейдите в **Workflows** → **Import from File**
3. Импортируйте по очереди:
   - `workflows/01-mqtt-data-pipeline.json`
   - `workflows/telegram-bot-commands.json`
   - `workflows/telegram-critical-alerts.json`
   - `workflows/telegram-callback-handler.json`
   - `workflows/05-data-rotation-cleanup.json`
   - `workflows/06-web-dashboard-simple.json`

### Шаг 4: Настройка credentials в workflows

В каждом импортированном workflow:

1. Откройте workflow
2. Найдите ноды с красным индикатором (не настроены credentials)
3. Выберите созданные ранее credentials:
   - PostgreSQL ноды → `Greenhouse PostgreSQL`
   - MQTT ноды → `MQTT Broker`
   - Telegram ноды → `UgAgro Telegram Bot`

### Шаг 5: Настройка Telegram Chat ID

В workflows `telegram-bot-commands.json` и `telegram-critical-alerts.json`:

1. Откройте нод "Отправить Оповещение" или "Отправить Статус"
2. В поле **Chat ID** замените на ваш реальный Chat ID
3. Сохраните workflow

### Шаг 6: Активация workflows

1. Откройте каждый workflow
2. Нажмите переключатель **Active** в правом верхнем углу
3. Убедитесь, что статус "Active" (зеленый)

---

## 🔧 Устранение Неполадок

### Ошибка: `toFixed is not a function`

**Причина**: PostgreSQL возвращает числа как строки

**Решение**: В коде используйте `parseFloat()` перед `.toFixed()`:
```javascript
const temp = parseFloat(reading.temperature) || 0;
const formatted = temp.toFixed(1);  // OK
```

### Ошибка: `$input.item is not a function`

**Причина**: Устаревший синтаксис n8n

**Решение**: Используйте новый синтаксис:
```javascript
// Старый (неправильно)
const data = $input.item(0).json;

// Новый (правильно)
const data = $input.first().json;
```

### Telegram бот не отвечает

**Решение**:
1. Проверьте TOKEN и CHAT_ID
2. Убедитесь, что написали боту `/start`
3. Проверьте что workflow активен (зеленый переключатель)
4. Проверьте логи n8n: `docker logs n8n`

### PostgreSQL подключение не работает

**Решение**:
1. Проверьте credentials: Host должен быть `postgres` (Docker service name)
2. Проверьте что контейнер PostgreSQL запущен: `docker ps`
3. Проверьте пароль в docker-compose.yml
4. Тест подключения: `docker exec -it postgres psql -U root -d n8n`

---

## 📝 Структура проекта

```
n8n-greenhouse-integration/
├── README.md                          # Этот файл
├── database/
│   └── init-greenhouse-db.sql         # Схема PostgreSQL
├── docs/
│   ├── BEGET_VPS_DEPLOYMENT.md       # Развертывание на VPS
│   ├── MQTT_DATA_STRUCTURE.md        # Структура MQTT данных
│   ├── POSTGRES_SETUP.md             # Настройка PostgreSQL
│   └── PUTTY_SETUP.md                # Настройка через SSH
└── workflows/
    ├── 01-mqtt-data-pipeline.json
    ├── telegram-bot-commands.json
    ├── telegram-critical-alerts.json
    ├── telegram-callback-handler.json
    ├── 05-data-rotation-cleanup.json
    └── 06-web-dashboard-simple.json
```

---

## 🔄 Обновления

### v2.0.0 (2025-01-18)

- ✅ Переход с JSON файлов на PostgreSQL
- ✅ Полная система Telegram бота (команды, оповещения, callbacks)
- ✅ Поддержка подтверждения и временного отключения оповещений
- ✅ Улучшенный веб-дашборд с Chart.js
- ✅ Автоматическая ротация данных через PostgreSQL функции
- ✅ Исправлена ошибка `.toFixed is not a function`
- ✅ Обновлен синтаксис для n8n v1.115.3

### v1.0.0 (2025-01-18)

- Первый релиз (JSON файлы - устарел)

---

## 📄 Лицензия

MIT License

---

## 👨‍💻 Автор

Разработано для проекта UgAgro 95 - автономная система управления теплицей на базе ESP32.

**ESP32 Repository**: https://github.com/Daydyaefim/Balagan

---

## 🤝 Поддержка

Если возникли проблемы:

1. Проверьте раздел [Устранение Неполадок](#устранение-неполадок)
2. Проверьте логи n8n: `docker logs n8n`
3. Проверьте логи PostgreSQL: `docker logs postgres`
4. Откройте issue в GitHub
